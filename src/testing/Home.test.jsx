import { render, screen } from '@testing-library/react'
import { describe, test, expect } from 'vitest'
import Home from '../components/Home'

describe('Home Page', () => {
  test('prueba de setup - debería pasar siempre', () => {
    expect(true).toBe(true)
  })
})